#include<stdio.h>
#include<stdlib.h>
#include<math.h>

int main()
{
    int num,x=1;
    scanf("%d",&num);
    if(num==2)
    {
        printf("The no. %d is prime\n",num);
    }
    for(num,x;x<num;num%x)
        if(num==)
    return 0;
}
