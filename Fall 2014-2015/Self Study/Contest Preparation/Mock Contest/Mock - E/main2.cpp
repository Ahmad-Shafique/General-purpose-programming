
#include <iostream>
#include <cstdio>

using namespace std;

int main()
{
    int T,N,op=0;
    scanf("%d",&T);
    for(int i=0; i<T ; i++)
    {
        scanf("%d",&N);
        int a[N],b[N],c[N],d[N];
        for(int j=0 ; j<N ; j++)
        {
            scanf("%d",&a[j]);
        }
        for(int j=0 ; j<N ; j++)
            scanf("%d",&a[j]);
        for(int k=0;k<N;k++)
        {
            int l=k;
            for(;l<N;l++)
            {

            }
        }
        printf("Case %d: %d",i+1,op);
    }
    return 0;
}
