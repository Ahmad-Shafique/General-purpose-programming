//Traditional Insertion Sort (27/4/15)


///Doesn't Work

#include <iostream>

using namespace std;

/*[1]int insertion_sort(int arr[],int arr_size)
{
    int value;
    bool inserted = false;
    cin>>value;
    if(arr[0] == 0)
    {
        arr[0] = value;
    }
    else
    {
        for(int i=0 ; i<arr_size ; i++)
        {
            cout<<"1...  ";
            if(arr[i] == 0)
            {
                arr[i] == value;
                inserted = true;
                return 0;
            }
        }
        if(inserted == false)
        {
            cout<<"No space left";
        }

    }
    return 0;
}*/

int main()
{
    int arr[12];
    /*[1]for(int i=0; i<= 4; i++)
       insertion_sort(arr,12);*/
    return 0;
}

